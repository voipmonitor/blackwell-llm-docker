# DeepSeek-V4-Flash-0731 fixed probabilistic DSpark K5 serving profile.
#
# GPU KV storage is the default. Set LMCACHE_MODE=ram or LMCACHE_MODE=disk to
# enable engine-driven LMCache transfer. The standard TP2 GPU-KV profile uses
# 0.975 GPU memory utilization. The qualified one-million-token engine-driven
# LMCache profile uses 0.970 because its asynchronous transfer path has a
# separately measured runtime-memory contract. Automatic KV admission profiles
# the scheduler-reachable DeepSeek V4 attention peak before assigning the
# remaining memory to cache storage.
# InstantTensor uses owned CUDA tensors and the BUFFERED backend for DS4 model
# loading. Set LOAD_FORMAT=fastsafetensors only for an explicit deployment
# requirement; the Spark launcher selects that loader independently.
services:
  ds4:
    image: ${IMAGE:-voipmonitor/vllm:jovian-judgement-vllmf66599d-b12x15b6813-fi803c466-cu133-torch213-20260907-r9}
    container_name: ${NAME:-ds4-dspark-jovian-judgement-r9}
    entrypoint:
      - /usr/local/bin/lmcache-mp-wrapper.sh
      - /usr/local/bin/serve-ds4-flash.sh
    network_mode: host
    ipc: host
    init: true
    shm_size: ${SHM_SIZE:-32gb}
    gpus: all
    restart: unless-stopped
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 1048576
        hard: 1048576
      stack: 67108864
    environment:
      CUDA_VISIBLE_DEVICES: ${GPUS:-0,1}
      GLOO_SOCKET_IFNAME: ${GLOO_SOCKET_IFNAME:-lo}
      NCCL_SOCKET_IFNAME: ${NCCL_SOCKET_IFNAME:-lo}
      OMP_NUM_THREADS: ${OMP_NUM_THREADS:-2}
      PORT: ${PORT:-8000}
      SERVED_MODEL_NAME: ${SERVED_MODEL_NAME:-DeepSeek-V4-Flash-0731}
      MODE: dspark
      BACKEND: ${BACKEND:-b12x-a8-dglin}
      TP_SIZE: ${TP_SIZE:-2}
      DCP_SIZE: ${DCP_SIZE:-1}
      ALLREDUCE_MODE: ${ALLREDUCE_MODE:-auto}
      DSPARK_DEPTH_MODE: ${DSPARK_DEPTH_MODE:-fixed}
      DSPARK_TOKENS: ${DSPARK_TOKENS:-5}
      DSPARK_CAPACITY_ACTIVATION_BATCH_SIZE: ${DSPARK_CAPACITY_ACTIVATION_BATCH_SIZE:-0}
      DRAFT_SAMPLE_METHOD: ${DRAFT_SAMPLE_METHOD:-probabilistic}
      MAX_NUM_SEQS: ${MAX_NUM_SEQS:-8}
      GRAPH: ${GRAPH:-auto}
      MAX_MODEL_LEN: ${MAX_MODEL_LEN:-1048576}
      MAX_NUM_BATCHED_TOKENS: ${MAX_NUM_BATCHED_TOKENS:-4096}
      GPU_MEMORY_UTILIZATION: ${GPU_MEMORY_UTILIZATION:-}
      LMCACHE_MODE: ${LMCACHE_MODE:-off}
      LMCACHE_TRANSFER_MODE: ${LMCACHE_TRANSFER_MODE:-engine_driven}
      LMCACHE_L1_GB: ${LMCACHE_L1_GB:-24}
      LMCACHE_CHUNK_SIZE: ${LMCACHE_CHUNK_SIZE:-4096}
      LMCACHE_SEPARATE_OBJECT_GROUPS: ${LMCACHE_SEPARATE_OBJECT_GROUPS:-1}
      LMCACHE_L2_GB: ${LMCACHE_L2_GB:-256}
      LMCACHE_L2_PATH: ${LMCACHE_L2_PATH:-/cache/lmcache/8000}
      LMCACHE_HTTP_PORT: ${LMCACHE_HTTP_PORT:-8099}
      LOAD_FORMAT: ${LOAD_FORMAT:-instanttensor}
      INSTANTTENSOR_BACKEND: ${INSTANTTENSOR_BACKEND:-BUFFERED}
      PYTHONHASHSEED: ${PYTHONHASHSEED:-0}
      EXTRA_VLLM_ARGS: ${EXTRA_VLLM_ARGS:-}
    volumes:
      - ${HF_CACHE:-/root/.cache/huggingface}:/root/.cache/huggingface
      - ${MODEL_ROOT:-/root/models}:/root/models:ro
      - ${JIT_CACHE:-./cache/jovian-judgement-ds4-r9}:/cache
      - ${CONTAINER_TMP:-./cache/jovian-judgement-ds4-r9/tmp}:/container-tmp
